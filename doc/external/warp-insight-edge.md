# External Dependency Note

当前部分模型文档仍依赖 `warp-insight` 中的边缘发现与上报设计背景。

在 `dayu-topology` 独立化阶段，这些内容暂不在本 repo 内完整重建，先统一通过本说明占位。

依赖主题主要包括：

- edge resource discovery runtime
- discovery 与 runtime state 的边界
- discovery snapshot 上报背景

后续若 `dayu-topology` 需要完全脱离原仓库，再把这些背景文档迁入本 repo 或重写为独立版本。
